{
	"name": "FlowOne",
	"properties": {
		"type": "MappingDataFlow",
		"typeProperties": {
			"sources": [
				{
					"dataset": {
						"referenceName": "SourceCsv",
						"type": "DatasetReference"
					},
					"name": "SourceBlob"
				}
			],
			"sinks": [],
			"transformations": [
				{
					"name": "RemoveColumns",
					"description": "Removing columns"
				},
				{
					"name": "NameClean",
					"description": "Cleaning Name"
				},
				{
					"name": "lowercase"
				}
			],
			"scriptLines": [
				"source(output(",
				"          Name as string,",
				"          Modified as string,",
				"          {Modified By} as string,",
				"          {S�kerhetsdatablad} as string,",
				"          {lg_complaint.activityid} as string,",
				"          {opportunity.opportunityid} as string,",
				"          {quote.quoteid} as string,",
				"          {salesorder.salesorderid} as string,",
				"          {Item Type} as string,",
				"          Path as string",
				"     ),",
				"     allowSchemaDrift: true,",
				"     validateSchema: false,",
				"     ignoreNoFilesFound: false) ~> SourceBlob",
				"SourceBlob select(mapColumn(",
				"          Name,",
				"          OID = {opportunity.opportunityid},",
				"          QID = {quote.quoteid},",
				"          SID = {salesorder.salesorderid},",
				"          Path",
				"     ),",
				"     skipDuplicateMapInputs: true,",
				"     skipDuplicateMapOutputs: true) ~> RemoveColumns",
				"RemoveColumns derive(Name = iif(size(split(Name, '\\r\\n')) > 1 , split(Name, '\\r\\n')[1], Name)) ~> NameClean",
				"NameClean derive(OID = lower(OID),",
				"          QID = lower(QID),",
				"          SID = lower(SID)) ~> lowercase"
			]
		}
	}
}